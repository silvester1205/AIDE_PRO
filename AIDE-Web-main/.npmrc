# Prevent npm from creating package-lock.json conflicts
package-lock=true

# Use exact versions for better reproducibility
save-exact=false

# Faster installs
prefer-offline=true

# Security
audit=true
